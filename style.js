import {StyleSheet} from 'react-native';

export default exStyles = StyleSheet.create({
    textBox: {
      color: 'black',
      fontWeight: 'bold',
      fontSize: 40,
    },
    bg: {
        backgroundColor: 'orange',
    },
  });